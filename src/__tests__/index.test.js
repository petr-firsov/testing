/*
@jest-environment jsdom
*/

import { validateCardNumber } from "../validatecardnumber";
import { checkCardSystem } from "../checkcardsystem";

test('input value is NaN', () => {
});

test('non existing payment system', () => {
});